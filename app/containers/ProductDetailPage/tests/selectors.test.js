// import { selectProductDetailPageDomain } from '../selectors';

describe('selectProductDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
